import fs from 'node:fs';

function replaceOnce(path, oldText, newText, marker) {
  let text = fs.readFileSync(path, 'utf8');
  if (text.includes(marker)) {
    console.log(`skip ${path}: already applied`);
    return;
  }
  const i = text.indexOf(oldText);
  if (i < 0) throw new Error(`target not found: ${path}`);
  text = text.slice(0, i) + newText + text.slice(i + oldText.length);
  fs.writeFileSync(path, text);
  console.log(`updated ${path}`);
}

const common = 'CONTENT_GUIDELINES.md';
replaceOnce(
  common,
  'この規約は `textbook/`、`statistical-mathematics/`、`applied-rikou-80/` に全面適用する。Ankiでは通常カードすべてに長い記号宣言を要求しないが、**定義・定理そのものを問うカード**では同じ規約に従う。\n\n### 3.2 共通基礎定理を演習で前提にする場合',
  `この規約は \`textbook/\`、\`statistical-mathematics/\`、\`applied-rikou-80/\` に全面適用する。Ankiでは通常カードすべてに長い記号宣言を要求しないが、**定義・定理そのものを問うカード**では同じ規約に従う。\n\n#### 3.1.1 定義・命題・補題・定理のステートメント自己完結性\n\n定義・命題・補題・定理は、**そのステートメントだけを切り出して読んでも、何について何を主張しているかが確定する**ように書く。直前の説明文や前の定理を読まないと、対象・記号・仮定・結論のいずれかが欠ける書き方をしない。\n\n各ステートメントでは必要に応じて次を内部で宣言する。\n\n- 対象となる集合・空間・確率空間・関数・確率変数・母数\n- ステートメント内で新たに使う記号とその意味\n- 成立に必要な仮定（可測性、可積分性、独立性、σ有限性、連続性、滑らかさ、compactness等）\n- 結論\n\n例えば「同じ仮定の下で」「上の $f$ について」「前節の記号を用いると」だけで始めて、ステートメント外に仮定や対象を預けない。共通の大域設定を再利用する場合でも、**その定理を適用するために必要な条件がステートメント内から判定できること**を最低条件とする。\n\n証明・直感説明・例はステートメントの外に置いてよいが、ステートメント自体の意味を補うためにそれらを必読にはしない。\n\n### 3.2 共通基礎定理を演習で前提にする場合`,
  '#### 3.1.1 定義・命題・補題・定理のステートメント自己完結性'
);

const audit = 'textbook/f0-dream-theater-content-exercise-audit.md';
replaceOnce(
  audit,
  '## 2. 定義・定理の明示性\n\n### 2.1 定義',
  `## 2. 定義・定理の明示性\n\n### 2.0 ステートメントはその中で完結させる\n\n定義・命題・補題・定理は、通常教材と同じく **ステートメント単体で自己完結**させます。読者が直前の段落を読み返さないと対象・記号・仮定・結論を復元できない書き方は監査NGです。\n\n監査時は各ステートメントについて、少なくとも次を確認します。\n\n- 何を対象にした定義・主張か\n- 新しい記号がその中で宣言されているか\n- 必要な仮定がその中に列挙されているか\n- 結論が明確か\n- 「同じ仮定の下で」「上の関数について」など、ステートメント外へ本質条件を預けていないか\n\n証明や直感説明は外に置いて構いません。ただし、**ステートメントの意味を完成させるために証明本文を読む必要がある状態**にはしません。\n\n### 2.1 定義`,
  '### 2.0 ステートメントはその中で完結させる'
);

replaceOnce(
  audit,
  '1. 主要概念が定義として明示されている。\n2. 主要命題の仮定と結論が明示され',
  '1. 主要概念が定義として明示され、定義文だけで対象・記号・条件が確定する。\n2. 主要命題・補題・定理はステートメント単体で対象・仮定・結論が自己完結している。\n3. 主要命題の仮定と結論が明示され',
  '主要命題・補題・定理はステートメント単体で対象・仮定・結論が自己完結している'
);
