from pathlib import Path

ROOT = Path(__file__).resolve().parents[1] / 'textbook/volumes/00_foundations'


def apply(dirname, replacements):
    p = ROOT / dirname / 'index.md'
    text = p.read_text(encoding='utf-8')
    for old, new in replacements:
        if old not in text:
            raise SystemExit(f'missing pattern in {dirname}: {old[:120]!r}')
        text = text.replace(old, new, 1)
    p.write_text(text, encoding='utf-8')


apply('F0_00A_集合_写像_上限下限', [
    ('\n命題\n$$\n\\forall x\\in\\mathbb R\\ \\exists y\\in\\mathbb R:\\ y>x\n$$\nを日本語で読み、その否定を量化記号で書け。',
     '\n次の主張\n$$\n\\forall x\\in\\mathbb R\\ \\exists y\\in\\mathbb R:\\ y>x\n$$\nを日本語で読み、その否定を量化記号で書け。'),
    ('元の命題は「任意の実数 $x$ に対して、それより大きい実数 $y$ が存在する」である。',
     '元の主張は「任意の実数 $x$ に対して、それより大きい実数 $y$ が存在する」である。'),
    ('これは「最大の実数が存在する」という主張であり、元の命題の否定になっている。',
     'これは「最大の実数が存在する」という主張であり、元の主張の否定になっている。'),
])

apply('F0_00A1_上界_下界_supremum_infimum', [
    ('$A=(-3,5)$ について $\\sup A,\\inf A,\\max A,\\min A$ を求めよ。',
     '$A=(-3,5)$ について $\\sup A,\\inf A$ を求め、それぞれの値が $A$ の元として達成されるか判定せよ。'),
    ('ただし端点 $-3,5$ は $A$ に含まれないので最大元・最小元は存在しない。',
     'ただし端点 $-3,5$ は $A$ に含まれないので、どちらの値も $A$ の元としては達成されない。'),
    ('$\\max A,\\min A$ はともに存在しない。',
     '$5,-3$ はともに $A$ に属さないため、上限・下限はいずれも集合内では達成されない。'),
    ('### F0-00A1-C01 Minkowski和の上限',
     '### F0-00A1-C01 集合の和 $A+B$ の上限'),
])

apply('F0_00A1D_順序_全順序_最小最大_整列', [
    ('### F0-00A1D-B03 半順序で最小元は一意',
     '### F0-00A1D-B03 最小元は一意'),
    ('半順序集合 $(P,\\preceq)$ に最小元が存在するとき、それが一意であることを示せ。',
     '集合 $P$ 上の関係 $\\preceq$ が反射律・反対称律・推移律を満たすとする。最小元が存在するとき、それが一意であることを示せ。'),
    ('半順序の反対称律から', '反対称律から'),
])

apply('F0_00B0_点列_部分列_十分大きい添字', [
    ('$n_1$ を $I$ の最小元とし、$n_k$ まで取ったら、$I$ は無限なので $n_k$ より大きい元が存在し、その最小のものを $n_{k+1}$ とする。',
     '$n_1\\in I$ を一つ取る。$n_k$ まで取ったら、$I$ は無限なので $n_k$ より大きい $I$ の元が存在する。その一つを $n_{k+1}$ とする。'),
    ('$a$ が現れる添字を小さい順に $n_1<n_2<\\cdots$ と並べれば $x_{n_k}=a$ の定数部分列になる。',
     '$a$ が現れる添字から、直前より大きいものを順に選べば $n_1<n_2<\\cdots$ かつ $x_{n_k}=a$ の定数部分列になる。'),
    ('命題 $P(n)$ が十分大きい $n$ で成り立つとする。',
     '条件 $P(n)$ が十分大きい $n$ で成り立つとする。'),
    ('### F0-00B0-B03 無限回起こる命題だけを抜き出す',
     '### F0-00B0-B03 無限回起こる条件だけを抜き出す'),
    ('命題 $P(n)$ が無限回起こるとする。',
     '条件 $P(n)$ が無限回起こるとする。'),
    ('命題 $P(n)$ について、次を示せ。',
     '条件 $P(n)$ について、次を示せ。'),
])

apply('F0_00C_連続写像_コンパクト性_最大最小', [
    ('$\\delta=\\varepsilon$ と取れば逆三角不等式から直ちに従う。',
     '$\\delta=\\varepsilon$ と取り、問題文で与えた不等式を使えば直ちに従う。'),
    ('$x\\in\\overline A$ を取る。距離空間では閉包の点列特徴付けにより、ある列 $a_n\\in A$ が存在して $a_n\\to x$。連続性から',
     '$x\\in\\overline A$ を取る。各 $n$ について閉包の定義から $A\\cap B(x,1/n)\\ne\\varnothing$ なので、$a_n\\in A\\cap B(x,1/n)$ を一つ取る。すると $a_n\\to x$。連続性から'),
    ('各 $f(a_n)\\in f(A)$ なので、再び閉包の点列特徴付けから $f(x)\\in\\overline{f(A)}$。従って包含が成り立つ。',
     '各 $f(a_n)\\in f(A)$ で $f(a_n)\\to f(x)$ だから、$f(x)$ の任意の近傍は十分大きい $n$ の $f(a_n)$ を含み、$f(A)$ と交わる。従って閉包の定義から $f(x)\\in\\overline{f(A)}$。よって包含が成り立つ。'),
    ('$x\\in\\overline A$ なら $a_n\\in A$, $a_n\\to x$ を取れる。連続性で $f(a_n)\\to f(x)$ だから $f(x)\\in\\overline{f(A)}$。',
     '$x\\in\\overline A$ なら各 $n$ で $a_n\\in A\\cap B(x,1/n)$ を取り、$a_n\\to x$ とできる。連続性で $f(a_n)\\to f(x)$ なので、$f(x)$ の任意の近傍が $f(A)$ と交わる。'),
])

apply('F0_00C1_コンパクト性_点列コンパクト性_Heine_Borel', [
    ('### F0-00C1-A04 コンパクト集合の連続像',
     '### F0-00C1-A04 有限部分被覆性は連続像で保たれる'),
    ('$K$ をコンパクト空間、$f:K\\to Y$ を連続写像とする。$f(K)$ がコンパクトであることを開被覆の定義から示せ。',
     '位相空間 $K$ の任意の開被覆が有限部分被覆を持つとする。連続写像 $f:K\\to Y$ に対して、$f(K)$ の任意の開被覆も有限部分被覆を持つことを示せ。'),
    ('$K$ のコンパクト性から有限個 $i_1,\\ldots,i_m$ で',
     '$K$ の開被覆が有限部分被覆を持つという仮定から、有限個 $i_1,\\ldots,i_m$ で'),
    ('像の開被覆を逆像で $K$ の開被覆へ戻し、コンパクト性で有限化してから像へ戻す。',
     '像の開被覆を逆像で $K$ の開被覆へ戻し、有限部分被覆を取ってから像へ戻す。'),
    ('### F0-00C1-B03 コンパクト空間の閉部分集合',
     '### F0-00C1-B03 閉部分集合へ有限部分被覆性を移す'),
    ('コンパクト空間 $X$ の閉部分集合 $F\\subseteq X$ がコンパクトであることを示せ。',
     '位相空間 $X$ の任意の開被覆が有限部分被覆を持つとする。閉部分集合 $F\\subseteq X$ の任意の開被覆も有限部分被覆を持つことを示せ。'),
    ('$X$ の開被覆である。コンパクト性で有限部分被覆を取り、最後に $F$ と交差すれば',
     '$X$ の開被覆である。仮定により有限部分被覆を取り、最後に $F$ と交差すれば'),
])

apply('F0_00D_Cauchy列_完備性_無限次元', [
    ('### F0-00D-A04 収束列からCauchy評価を再構成する',
     '### F0-00D-A04 収束列からCauchy条件を再構成する'),
])

print('audit wording fixes applied')
