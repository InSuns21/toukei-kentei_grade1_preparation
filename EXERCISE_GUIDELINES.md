# 統計数理・統計応用 共通演習規約

このファイルを、次の2問題集に共通する作問・解答・採点・表記規約の**唯一の正本**とする。

- `statistical-mathematics/`
- `applied-rikou-80/`

各ディレクトリの `README.md` は構成・学習順・実装状況だけを扱い、ここに定める規約を別定義しない。規約を変更するときは本ファイルだけを更新し、両問題集の本文を同時に監査する。

## 1. 共通フォーマット

各大問は原則として次を持つ。

1. 問題
2. 詳細解答
3. 本番答案
4. 採点基準（20点満点）

必要に応じて「打ち切り判断」「補足」を追加してよいが、上記4区分の代替にはしない。

## 2. 詳細解答で埋める行間

「詳細解答」は、初見の受験者が途中式から答案を再現できる粒度とする。次を必須とする。

- 設問が「求めよ・導け・示せ」と要求した式を、詳細解答の冒頭で既知公式として置かない。
- 採点対象の式は、定義、和・積分、変数変換、微分、平方完成、正規方程式、特性方程式など、得点につながる出発点から導く。
- 1行の変形に複数の非自明な操作が入る場合は分ける。少なくとも「何を代入したか」「どの恒等式を使ったか」「どの項が消えたか」を追えるようにする。
- 「整理すると」「同様に」「独立性より」だけで、複数段の代数・期待値計算・積分計算を飛ばさない。
- 和や積分から確率分布を導く問題では、支持集合、和・積分の範囲、正規化を必要に応じて明示する。
- 変数変換では、変換式、逆変換、ヤコビアン、変換後の支持集合のうち必要なものを省略しない。
- 漸近展開では、展開点、剰余項の次数、確率収束・分布収束をつなぐ理由を示す。
- 問題文が使用を明示的に許可した定理・公式・分位点は証明不要。ただし、それを本問へ適用した後の代数は省略しない。

### 分布から尤度・情報量を作る問題の粒度

確率密度関数、確率質量関数、尤度、対数尤度、スコア、フィッシャー情報量の導出が主題または主要な採点対象になる問題では、**分布名から途中式を暗記で補わせない**。

- 公式過去問の出題形式を優先し、計算の出発点として必要な確率密度関数・確率質量関数は、原則として問題文に具体式を与える。`N(\mu,\sigma^2)` など分布記号だけで、密度式まで暗記済みと仮定しない。
- 詳細解答では、与えられた確率密度関数・確率質量関数 → 1標本の尤度 → 独立標本なら積による同時尤度 → 対数尤度 → 微分、の順を追えるように書く。
- 「対数尤度は定数を除いて」「比例定数を無視して」から詳細解答を始めない。まず完全な式を書き、**どの項が未知母数を含まないため微分・最大化に影響しないのか**を確認してから省略してよい。
- 密度と尤度が同じ数式でも見方が違うことを、初学者が迷う可能性がある問題では説明する。観測値を固定し、未知母数の関数として見たものが尤度であることを明示する。
- 既知の分布公式を使う場合でも、その公式自体が設問の採点対象、または後続計算の主要な出発点なら、問題文で与えるか詳細解答で定義・導出する。

### 本番答案との違い

本番答案は圧縮してよい。ただし、採点基準で独立に点数を与える導出の核は残す。「答えのみ」を満点答案にしない。

## 3. 定理・補題・漸近結果を使うとき

原則として次の4点を順に書く。

1. **定理名**：何を使うか。
2. **必要条件**：本問に関係する条件。
3. **条件確認**：その条件が本問で成立する理由。
4. **適用結果**：定理から何が従うか。

特に、十分性・完備性・不偏性、必要な期待値や積分が有限であること、共通支持、微分可能性、微分と積分または和の交換、真値が内部点であること、識別可能性、有限で正の情報量、独立同分布、有限分散、正規性、直交性、ランク、既約性・非周期性・正再帰性など、定理ごとの条件を落とさない。

定理名だけ、または条件だけを書いて適用を済ませたことにしない。**「この問題ではなぜ使えるか」までを答案の一部**とする。

### 測度論を前提にしない

問題、詳細解答、本番答案、採点基準、学習者向け補足のいずれでも、**測度論を既知知識として要求しない**。

- σ-加法族・σ-代数、可測性、確率測度、Lebesgue積分、Borel集合、Radon–Nikodymの定理、Fubiniの定理、Tonelliの定理、単調収束定理、優収束定理などを、導出や正当化の前提として使わない。
- 「ほとんど確実に」「概収束」「a.s.」「almost surely」など、測度論的な収束概念を説明なしに持ち込まない。統計検定1級で必要な範囲では、確率収束・分布収束・平均二乗収束など、シラバスに沿った表現で記述する。
- 微分と積分の交換、期待値と微分の交換、二重積分の順序交換などを厳密に正当化するために測度論が必要になる場合、その厳密化自体を採点対象にしない。問題文で「必要な交換が許される正則条件を仮定する」と与えるか、有限和・通常の積分計算など初等的に追える導出へ置き換える。
- 測度論を使えば短く書ける場合でも、詳細解答では確率、確率密度関数、確率質量関数、累積分布関数、期待値、和、通常の積分の範囲で説明する。
- 「厳密には測度論を使う」といった注記も、問題を解くために不要なら学習者向け本文へ入れない。

## 4. 電卓・数表の共通基準

統計数理・統計応用（理工学）の両方で、統計検定1級の試験条件に合わせる。

- 使用を前提としてよい電卓機能は、四則演算、百分率、平方根までとする。
- 関数電卓、プログラム電卓、グラフ電卓、コンピュータ代数システム、R、Python、数値積分を前提にしない。
- $\log$, $e^x$, 正規分布の累積分布関数、ガンマ関数、非心分布など、一般電卓だけでは数値評価できない量は記号式までで完答とするか、必要な定数・分位点を問題文で与える。
- $t$, $\chi^2$, $F$ などの分位点が必要なら、問題文で値を与えるか「問題冊子の統計数値表を用いてよい」と明記する。暗記を要求しない。
- 正確検定の有限和を手計算させる場合は、四則演算で追える項数に限定する。
- 主題でない特殊積分は値を与える。積分そのものが論点なら、置換・部分積分など手計算可能な範囲にする。

### 電卓・数表レビュー

各問題で次を確認する。

1. 立式後に許可範囲外の計算機能がないと数値が出ない箇所はないか。
2. その数値化は統計的能力の評価に必要か。
3. 必要なら数表・定数・分位点を問題文で与えたか。
4. 不要なら記号式までを完答としたか。
5. 詳細解答が手計算の本体を「整理すると」で隠していないか。

## 5. 表記は公式シラバスを正本とする

学習者向けの本文、見出し、表、問題文、詳細解答、本番答案、採点基準の用語は、**統計検定1級の公式出題範囲表（シラバス）の表記を第一優先**とする。

「英字略語を一律に禁止する」のではなく、次の順で表記を決める。

1. 公式シラバスが日本語の正式名称で掲載している用語は、その日本語表記を使う。
2. 公式シラバス自身が英字略語・英字を含む名称で掲載している用語は、その表記をそのまま使ってよい。
3. 時系列モデルの標準記号である `AR(p)`, `MA(q)`, `ARMA(p,q)`, `ARIMA(p,d,q)` は学習者向け本文・見出しでも使用してよい。必要なら初出で「自己回帰」「移動平均」を併記する。
4. シラバスにもなく、標準的なモデル記号でもない非自明な略語を教材側の都合だけで導入しない。

### シラバスに合わせてそのまま使ってよい代表例

公式出題範囲表には、たとえば次の表記が実際に掲載されているため、そのまま使用してよい。

- `MCMC`
- `ARIMA`（`ARIMAモデル` / `ARIMA過程`）
- `AIC`（情報量規準AIC）
- `BLUE`（線形推定 (BLUE)）

また、`AR(1)`, `AR(2)`, `MA(1)`, `ARMA(1,1)` などは、時系列モデルの標準記号として使用してよい。

### 日本語で書く代表例

公式シラバスが日本語名で掲載しているものは日本語に寄せる。

| 避ける略語 | 原則表記 |
| --- | --- |
| MGF | モーメント母関数 |
| PGF | 確率母関数 |
| PDF | 確率密度関数 |
| PMF | 確率質量関数 |
| CDF | 累積分布関数 |
| CF | 特性関数 |
| MLE | 最尤推定量、または文脈に応じて最尤法 |
| LRT | 尤度比検定 |
| CLT | 中心極限定理 |
| LLN | 大数の法則 |
| OLS / GLS | 通常最小二乗法 / 一般化最小二乗法 |
| UMVU | 一様最小分散不偏推定量 |
| UMP / UMPU | 一様最強力検定 / 一様最強力不偏検定 |
| MSE / RMSE | 平均二乗誤差 / 二乗平均平方根誤差 |
| VIF | 分散拡大係数 |
| ANOVA / ANCOVA | 分散分析 / 共分散分析 |
| PCA | 主成分分析 |
| LDA / QDA | 線形判別分析 / 二次判別分析 |
| ACF / PACF | 自己相関関数 / 偏自己相関関数 |
| MTTF / MTTR / MTBF | 平均故障時間 / 平均修復時間 / 平均故障間隔 |
| CUSUM / EWMA | 累積和 / 指数加重移動平均 |
| ICC | 級内相関係数 |
| BIC | ベイズ情報量規準 |
| FDR / FWER | 偽発見率 / 家族内誤差率 |

`CDF` の日本語名は **累積分布関数** とする。「累積密度関数」とは書かない。

### 数式記号との境界

数式中で定義済みの記号は日本語へ置換しない。たとえば誤差平方和を本文では「誤差平方和」と書き、式の中で $SSE$ と置いたなら、その後の数式では $SSE$ を使ってよい。

一方、シラバスに日本語名しかない概念について、見出しや説明文を `SSE`, `ACF` などだけで済ませることは避ける。

### 例外

- 公式シラバスに掲載された英字表記
- `AR(p)`, `MA(q)`, `ARMA(p,q)`, `ARIMA(p,d,q)` など標準的なモデル記号
- 数式中の標準的な記号・定義済み記号（$t$, $F$, $\chi^2$, $N(\mu,\sigma^2)$, $SSE$ など）
- ファイル名、URL、プログラムコード、コマンド名
- 固有名詞・製品名

は表記検査の禁止対象外とする。

## 6. レビュー時の共通チェック

両問題集で同じ観点を使う。

- 問題が要求した式を既知公式として先取りしていないか。
- 尤度・対数尤度・情報量が主題なのに、問題文で必要な確率密度関数・確率質量関数を与えず暗記前提にしていないか。
- 詳細解答が「定数を除いて」「比例定数を無視して」から始まり、密度→尤度→対数尤度の流れを飛ばしていないか。
- 詳細解答の各変形を目で追えるか。
- 定理名・必要条件・条件確認・適用結果がそろっているか。
- 測度論を既知知識として要求する記述や、不要な測度論用語が混入していないか。
- 微分・積分・期待値の交換を、測度論の定理を前提に正当化していないか。
- 電卓・数表の前提が試験条件を超えていないか。
- 用語表記が公式シラバスに寄っているか。
- シラバスにも標準モデル記号にもない非自明な略語を独自導入していないか。
- 数式記号まで不自然に日本語化していないか。
- 本番答案に採点上必要な導出が残っているか。
- 採点基準と本番答案の内容が対応しているか。

この規約に反する既存本文を見つけた場合は、局所規約を追加するのではなく、本文を本規約へ合わせる。