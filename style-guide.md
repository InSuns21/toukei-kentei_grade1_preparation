# 執筆スタイルガイド

## 読者と到達点

読者は大学初年度の微積分・線形代数を履修済みで、統計検定1級を独習する受験者とする。直観と厳密な議論を分け、最終的には20〜30分で採点可能な論述答案を書ける状態へ導く。

## 章の二層構造

- 詳細解説では、定義、仮定、方針、中間計算、結論、検算を省略しない。
- 数式・論理展開の「行間を少なくする」。すなわち、非自明な代入、展開、微分、積分、極限移行、同値変形、場合分けを読者の暗算に委ねず、施した操作とその根拠を一段ずつ示す。
- 一つの等号・含意に複数の主要操作を詰め込まない。等号変形と一方向の含意、必要条件と十分条件を明確に区別する。
- 本番答案では、得点に必要な立式・根拠・結論を残して圧縮する。
- Level C以上の解答には小問別の目安時間と「25分経過時の打ち切り判断」を付ける。

## 問題レベル

- Level A: 5〜10分の単一技能。
- Level B: 10〜15分の2〜3小問。
- Level C: 20〜30分、4〜6小問の本番標準。
- Level D: 30〜45分、理解を深める発展問題。

標準配分は A 20%、B 25%、C 45%、D 10% とする。久保川資料の章末問題は上限の校正に使い、Level Cは原則としてそれより少し短い計算量にする。

## 過去問準拠の大問構造

- Level A/Bは単一技能の訓練としてよい。Level C/Dと30分ドリルは、原則4〜6小問を一つの設定で連結する。
- 本番標準問題は「分布・確率の導出」「モーメントまたは行列計算」「推定・検定・条件付き量への接続」「結論の解釈」のうち少なくとも3段階を含む。
- 後続小問は前の結果を再利用できる誘導にする。ただし前半を落としても、与えられた結果を使って後半の部分点を取れる救済を設ける。
- 章内知識だけで完結させるために実際の出題傾向を単発技能へ切り縮めない。未修単元へ接続する小問は「先取り」または「後続章で再挑戦」と明示する。
- 各章の `chapter.yaml` に `past_exam_alignment` を置き、年度・科目・大問ID、テーマ、現在の章で担当する技能、後続技能を記録する。
- `08_exam_drill.md` には対応する実過去問IDと、設定・数値・設問を独自化した変更点を書く。
- `09_past_exam_practice.md` では実過去問を正式な演習課題として指定する。問題文・図表は転載せず、公式公開PDFまたは公式問題集を参照させる。

## 統計記述の必須事項

- 分布名だけでなく、台、パラメータ範囲、密度または質量関数を明記する。
- 「正則条件の下で」で済ませず、実際に必要な微分可能性、積分交換、情報量の有限性などを列挙する。
- 漸近論では $n\to\infty$、固定する量、正規化、極限分布を明記する。
- 検定では仮説、統計量、帰無分布、棄却域、有意水準、必要なら検出力を区別する。
- 推定量と推定値、確率変数と観測値、母数と統計量を混同しない。
- 数値計算は可能なら別計算またはスクリプトで検算し、丸め規則を示す。

## Markdown・KaTeX

- 見出しレベルを飛ばさず、HTMLでレイアウトしない。
- インライン数式は `$...$`、別行立ては `$$...$$`。
- 複数行は `$$\begin{aligned}...\end{aligned}$$`。
- `\(...\)`、`\[...\]`、`align`、`equation`、独自マクロ、外部パッケージ依存は禁止。
- Unicode数式記号を本文と数式で不用意に混在させない。

## 問題と解答

各問題に安定ID、Level、目安時間、主題、使用技術、計算量を付ける。詳細解答は全小問と一対一に対応させ、「方針」「使用結果と仮定」「計算・証明」「結論」「検算」「本番答案」「採点基準」を含める。

過去問由来の構造を使う場合は、出典の年度・問題番号、確認元、確度、変更点を記録する。第三者サイトのテーマ一覧は `third_party_topic_index`、公式問題・公式問題集を直接確認した場合だけ `official_problem` とする。原文を直接確認できないものを公式問題として表示しない。
