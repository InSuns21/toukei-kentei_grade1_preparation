name: Fix Batch 1 definition examples

on:
  push:
    branches:
      - audit/syllabus-term-coverage-batch1
    paths:
      - 'scripts/fix-batch1-definition-examples.mjs'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/syllabus-term-coverage-batch1
          fetch-depth: 0
      - name: Add missing definition verification blocks
        run: node scripts/fix-batch1-definition-examples.mjs
      - name: Commit fixes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          if ! git diff --quiet; then
            git add textbook
            git commit -m 'Add verification examples for Batch 1 definitions'
            git push origin HEAD:audit/syllabus-term-coverage-batch1
          fi
