name: Apply textbook concept DAG batch8 wording cleanup

on:
  push:
    branches:
      - audit/textbook-concept-dag-batch8-confidence-core
    paths:
      - .github/workflows/tmp-textbook-concept-dag-batch8-wording.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/textbook-concept-dag-batch8-confidence-core
          fetch-depth: 0
      - name: Apply exact edit
        run: python3 scripts/tmp_textbook_concept_dag_batch8_wording.py
      - name: Commit wording cleanup
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add textbook
          git diff --cached --check
          git commit -m 'Keep Batch 8 S1-03 bridge scoped to confidence coefficient'
          git push origin HEAD:audit/textbook-concept-dag-batch8-confidence-core
