name: Temporary F0-E2 display delimiter repair

on:
  push:
    branches:
      - fix/f0-e2-formal-theorems

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/tmp-fix-f0-e2-display-delimiters.mjs
      - name: Commit repair
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add textbook/volumes/00_foundations/F0_00E2_Cauchy_Schwarz_Bessel_Parseval/index.md
          if ! git diff --cached --quiet; then
            git commit -m "F0-00E2のformal数式区切りを修正する"
            git push
          fi
      - run: npm ci
      - run: npm run validate
      - run: npm run validate:pages
