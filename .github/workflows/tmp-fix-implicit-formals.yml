name: Temporary implicit formal fixes

on:
  push:
    branches:
      - fix/formal-anchors-and-definition-audit
    paths:
      - 'scripts/tmp-fix-implicit-formals.mjs'
      - '.github/workflows/tmp-fix-implicit-formals.yml'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/formal-anchors-and-definition-audit
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/tmp-fix-implicit-formals.mjs
      - name: Commit fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add textbook/volumes/00_foundations/F0_00D4_Lebesgue測度_Borel集合_拡張定理/index.md textbook/volumes/00_foundations/F0_00F1_固有空間_スペクトル定理_PSD/index.md
          git commit -m 'formalize implicit definitions in D4 and F1'
          git push
