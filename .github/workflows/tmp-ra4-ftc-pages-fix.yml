name: Temporary RA4 FTC Pages fix
on:
  push:
    branches:
      - fix/ra4-ftc-linkage
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/ra4-ftc-linkage
          fetch-depth: 0
      - name: Apply RA4 FTC Pages fix
        run: python scripts/tmp_ra4_ftc_pages_fix.py
      - name: Commit fix and remove temporary files
        run: |
          rm scripts/tmp_ra4_ftc_pages_fix.py .github/workflows/tmp-ra4-ftc-pages-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "RA4: express FTC linkage as terminology mapping"
          git push origin HEAD:fix/ra4-ftc-linkage
