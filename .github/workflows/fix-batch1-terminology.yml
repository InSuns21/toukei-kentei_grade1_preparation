name: Fix Batch 1 terminology

on:
  push:
    branches:
      - audit/syllabus-term-coverage-batch1
    paths:
      - 'scripts/fix-batch1-terminology.mjs'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/syllabus-term-coverage-batch1
          fetch-depth: 0
      - name: Use Japanese-primary ANCOVA terminology
        run: node scripts/fix-batch1-terminology.mjs
      - name: Commit fix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          if ! git diff --quiet; then
            git add textbook
            git commit -m 'Use Japanese-primary covariance analysis terminology'
            git push origin HEAD:audit/syllabus-term-coverage-batch1
          fi
