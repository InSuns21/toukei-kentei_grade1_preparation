schema_version: 1
batch_id: 2026-08-29-sampling-quality-dedupe-01
items:
  - id: samp-chisq-mean-variance
    bucket: duplicates
    reason: duplicate
    canonical_card: samp-chisq-mean-var-indep
    note: 両カードとも正規標本で (n-1)S^2/sigma^2 ~ chi^2_{n-1} を扱う。canonical側をCochran分解・独立性・観測Qの具体計算まで補強済みなので、公式再掲中心の重複側を統合する。
  - id: samp-max-distribution
    bucket: duplicates
    reason: duplicate
    canonical_card: dist-order-max
    note: どちらも最大値について M<=m iff 全Xi<=m から F_M(m)=F_X(m)^n を作り微分する同一move。canonical側はU(0,1), n=2でCDF→密度→support→積分1の確認まで実行しており、一般式を再掲するだけのsampling側より計算例が強い。
