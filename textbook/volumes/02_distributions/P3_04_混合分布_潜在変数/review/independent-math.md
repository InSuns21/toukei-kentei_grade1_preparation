# P3-04 独立数理査読

- 担当ID: `/root/p304_math_review`
- 実行モデル: `gpt-5.6-sol`
- 初回査読日時: 2026-08-11T12:45:35+09:00
- 対象: `00_overview.md` から `09_past_exam_practice.md`、`chapter.yaml`、`glossary.yaml`

## 独立再計算

### 有限混合と全分散

各 $g_j$ が確率質量関数または確率密度で、$\pi_j\geq0$、$\sum_j\pi_j=1$ なら
$$
\sum_x\sum_j\pi_jg_j(x)=\sum_j\pi_j=1
$$
または積分版の同じ計算から、周辺分布は正規化される。$E[|X|]<\infty$ のとき
$$
E[X]=\sum_j\pi_j\mu_j,
\qquad \mu_j=E[X\mid Z=j].
$$
$E[X^2]<\infty$ のとき
$$
\begin{aligned}
\operatorname{Var}(X)
&=E[\operatorname{Var}(X\mid Z)]+\operatorname{Var}(E[X\mid Z])\\
&=\sum_j\pi_jv_j+\sum_j\pi_j(\mu_j-\mu)^2.
\end{aligned}
$$
二成分では第2項が $\pi(1-\pi)(\mu_1-\mu_2)^2$ になる。本文の最終式は正しいが、これらの存在条件と導出が本文に欠ける。

### Poisson--Gamma混合

shape-rate表示のGamma密度を用いると
$$
\begin{aligned}
P(X=k)
&=\frac{\beta^\alpha}{k!\Gamma(\alpha)}
\int_0^\infty\lambda^{k+\alpha-1}e^{-(\beta+1)\lambda}\,d\lambda.
\end{aligned}
$$
$u=(\beta+1)\lambda$ と置けば $d\lambda=du/(\beta+1)$ なので
$$
\int_0^\infty\lambda^{k+\alpha-1}e^{-(\beta+1)\lambda}\,d\lambda
=\frac{\Gamma(k+\alpha)}{(\beta+1)^{k+\alpha}}.
$$
したがって掲載された周辺確率質量関数は正しい。また
$$
E[X]=E[\Lambda]=\frac\alpha\beta,
$$
$$
\operatorname{Var}(X)=E[\Lambda]+\operatorname{Var}(\Lambda)
=\frac\alpha\beta+\frac\alpha{\beta^2}
$$
も正しい。

### 正規混合と責務

二成分正規混合の平均・分散公式は正しい。P3L-B04では
$$
\tau_1(x)=\frac{\phi(x)}{\phi(x)+3\phi(x-3)}.
$$
よって
$$
\tau_1(0)=\frac{\phi(0)}{\phi(0)+3\phi(3)}\approx0.9677,
$$
$$
\tau_1(3)=\frac{\phi(3)}{\phi(3)+3\phi(0)}\approx0.00369.
$$
大小関係は解答どおりだが、$\tau_1(3)=1/4$ は誤りである。さらに $\phi(3/2)=\phi(-3/2)$ だから
$$
\tau_1(3/2)=\frac{1}{1+3}=\frac14
$$
であり、現在の問題文の「$1/2$ となる条件を確認」は成立しない。

### Poisson--Gamma事後分布と予測

$X=x$ と新しい $Y$ が同じ潜在率 $\Lambda$ の下で条件付き独立であると明記した場合、Bayes公式により
$$
\begin{aligned}
f(\lambda\mid X=x)
&\propto e^{-\lambda}\lambda^x
\lambda^{\alpha-1}e^{-\beta\lambda}\\
&=\lambda^{\alpha+x-1}e^{-(\beta+1)\lambda},
\end{aligned}
$$
したがって
$$
\Lambda\mid X=x\sim\operatorname{Gamma}(\alpha+x,\beta+1).
$$
さらに
$$
\begin{aligned}
P(Y=0\mid X=x)
&=E[e^{-\Lambda}\mid X=x]\\
&=\left(\frac{\beta+1}{\beta+2}\right)^{\alpha+x}.
\end{aligned}
$$
掲載結果はこの解釈なら正しい。一方、新しい $Y$ が独立な新しい潜在率に従うなら $X$ では更新されず、答えは $(\beta/(\beta+1))^\alpha$ である。問題文は両者を区別できていない。

### 推定と識別

$X_i$ が独立であるとの仮定の下では
$$
\operatorname{Var}(\bar X)=\frac{\alpha(\beta+1)}{n\beta^2},
\qquad
\operatorname{Var}(\beta\bar X)=\frac{\alpha(\beta+1)}n,
$$
であり、Chebyshev不等式による確率収束は正しい。ただし、潜在率の独立性だけでは観測の条件付き独立性は自動的に書面上確定しないため、問題文に仮定が必要である。

平均・分散だけで二成分正規混合の3母数が決まらないことは、未知数と式の個数だけでなく反例で示せる。例えば分散を各成分で1とすると、
$$
(\pi,\mu_1,\mu_2)=(1/2,-1,1)
$$
と
$$
(\pi,\mu_1,\mu_2)=(1/5,-2,1/2)
$$
はいずれも平均0、分散2だが異なる混合分布である。

## 初回指摘

### major

1. `06_exercises.md` のP3L-C01・P3L-D01、`08_exam_drill.md` — $\Lambda_i$ の独立同分布性だけが書かれ、$X_i$ の条件付き独立性が明記されていない。$\operatorname{Var}(\bar X)=\operatorname{Var}(X_i)/n$ を使うため、階層モデル全体の独立性を問題文に追加する。
2. `06_exercises.md` のP3L-D01(4)、`08_exam_drill.md` の(5) — $Y$ が $X_1$ と同じ $\Lambda_1$ を共有するのか、新しい独立な潜在率を持つのか不明で、二つの異なる答えが生じる。同じ率を共有し、$X_1,Y$ がその率の下で条件付き独立であると明記する。
3. `07_solutions.md` のP3L-B04 — $\phi(3)=\phi(0)$ とする誤計算があり、$\tau_1(3)=1/4$ は誤り。上の独立計算へ修正し、P3L-B04(3)の偽の前提も「$1/2$ か判定せよ」などへ改稿する。
4. `03_theorems.md` — 全分散、Poisson--Gamma周辺化、正規混合分散の結果だけが示され、正規化、期待値存在条件、Gamma積分の置換、群間分散の整理が省略されている。大学初年度読者が再現できる証明を一段ずつ追加する。
5. `07_solutions.md` と `08_exam_drill.md` — 詳細解答が「共役性より」など未導入の結果を直接使い、方針・仮定・中間計算・検算・本番答案・採点基準も大半で欠ける。特に事後Gamma密度はBayes公式から正規化まで導出する。
6. `07_solutions.md` のP3L-C05(4) — 「式が2つ、未知数が3つ」だけでは非識別の数学的実証として不十分。上記のように同じ平均・分散を持つ異なる母数組を少なくとも一組示す。

### minor

1. `02_definitions.md` — `$Lambda\sim...$` は `\Lambda` の脱字である。
2. `02_definitions.md` — 連続型でも一点の確率は通常0なので、$g(x)=0$ の説明を「その点は観測されない」ではなく「周辺密度が0の範囲では責務の値は結果に影響しない」とする。
3. `03_theorems.md` のP3L-THM-02 — $\alpha$ は任意の正実数なのに「必要成功回数 $r=\alpha$」と説明している。試行回数の解釈は $\alpha\in\mathbb N$ の場合に限るか、一般の $r>0$ の負の二項型と記す。
4. `04_examples.md` の例3 — 「値を代入して比較」としながら数値または比の整理まで完了していない。$\tau_1(1)=1/(1+3e^{-3/2})\approx0.599$ まで示す。
5. `07_solutions.md` のP3L-C03(5) — $(1-\bar X)/2$ は標本によって $[0,1]$ を外れる。モーメント方程式の形式解であること、母数空間内に制約するなら0と1へ切り詰めることを記す。
6. `07_solutions.md` のP3L-C04(2) — 微分方程式だけでは $n_1=0,n$ の境界最尤解を扱っていない。$0<n_1<n$ の内点計算と、両端の場合を分ける。

## 初回判定

`fatal: 0 / major: 6 / minor: 6`

## 修正後再査読

- 再査読日時: 2026-08-11T13:06:08+09:00
- 再査読範囲: 対象章全文、全14演習、全解答、P3L-DRILL-01、初回指摘後に追加された本番答案・時間判断・検算一覧
- `npm run validate`: 成功（KaTeX strict 160 Markdown、本文検査177ファイル）

### majorの解消確認

1. P3L-C01・D01・ドリルに、潜在率の独立同分布性と観測値の条件付き独立性が明記された。独立性を用いる標本平均の分散計算と一致する。
2. D01・ドリルは $X_1,Y$ が共有潜在率 $\Lambda_1$ の下で条件付き独立という一つのモデルに統一された。事後は $\operatorname{Gamma}(\alpha+x,\beta+1)$、予測確率は $((\beta+1)/(\beta+2))^{\alpha+x}$ となり、問題と解答が一致する。
3. P3L-B04は $\tau_1(3)=e^{-9/2}/(e^{-9/2}+3)\approx0.00369$ へ修正され、$x=3/2$ では事前重みにより $1/4$ となる設問へ改稿された。
4. 全分散の交差項、Gamma積分、正規混合分散、観測尤度と責務の導出が追加された。期待値には $E[|X|]<\infty$、分散には $E[X^2]<\infty$ が明記された。
5. Poisson--Gamma事後分布は尤度と事前密度の積から導出され、C/D本番答案、採点、時間判断、ドリル詳細解答、全14問の方針・仮定・検算一覧が追加された。
6. P3L-C05は平均0・分散2を共有する二つの異なる母数組を、$d=4/\sqrt3$ として正しく構成した。$1+(3/16)d^2=2$ を再計算して確認した。

### minorの解消確認

1. `\Lambda` の脱字は修正された。
2. 周辺質量または密度が0の範囲では責務の値が確率計算に影響しない、という説明へ修正された。
3. 負の二項型の $r=\alpha$ は形状パラメータであり、非整数の場合は解析的拡張であると明記された。
4. 例3は $\tau_1(1)\approx0.599$ まで計算された。
5. P3L-C03は形式推定量と $[0,1]$ への制約付き推定量を区別し、本番答案とも一致した。
6. P3L-C04は $0<n_1<n$ の内点と $n_1=0,n$ の境界を場合分けした。

### 全文再計算の最終確認

- 混合質量・密度、責務、全期待値、全分散、Poisson--Gamma周辺分布の式は相互に整合する。
- P3L-A01--D01の全小問を再計算し、掲載結論と一致した。
- 共有潜在率による事後予測と、独立な新規潜在率の場合の違いが定義上区別された。
- 演習ID、解答、完成形答案、検算一覧は一対一に対応する。
- 新規の数理的指摘はない。

## 最終判定

`fatal: 0 / major: 0 / minor: 0`

## 詳細解答再改訂後の独立査読

- 担当ID: `/root/p304_detail_math_review`
- 実行モデル: `gpt-5.6-sol`
- 実行日時: 2026-08-11T17:00:33+09:00
- 対象: 改訂後の章全文、全14演習と全解答、追加された「独習用の逐行補足」、本番答案、30分ドリル
- `npm run validate`: 成功（KaTeX strict 199 Markdown、本文検査222ファイル）

### 独立再計算の結果

- A01--A04、B01--B04の全確率・全期待値・全分散・責務を再計算し、掲載値と一致した。B02の周辺質量は $9(k+1)/4^{k+2}$ で、級数和も1になる。
- C01のPoisson--Gamma周辺質量、平均、分散、標本平均の分散、Chebyshev不等式を再計算した。章のPoisson--Gamma複数標本に関する共通仮定を適用すれば結論は正しい。
- C02の責務は $\tau_1=8e^3/(8e^3+125)$、予測確率は $(8e+125e^{-5})/(8e^3+125)$ となり、掲載結論と一致した。
- C03の分類閾値は0、誤分類確率は $\Phi(-1)$、混合比の形式推定量は $(1-\bar X)/2$ であり、掲載結論と一致した。
- C04の最尤推定量、観測尤度、責務の式自体は独立標本という追加仮定の下で正しい。
- C05の二つの母数組はいずれも平均0、分散2を与え、ラベル交換とは別のモーメント非識別例として正しい。
- D01の周辺分布、モーメント、不偏性・一致性、事後Gamma分布、共有潜在率による予測確率を再計算し、掲載結論と一致した。

### major

1. `06_exercises.md` P3L-C04 — 完全データ尤度と観測データ尤度を積で書くために必要な $(X_i,Z_i)$ の標本間独立性が問題文にない。`03_theorems.md` の定理は「独立な観測」を仮定しているが、C04の設定はその仮定を明示せず、Poisson--Gamma節の共通独立性も正規混合には適用されない。問題文に「$(X_i,Z_i)$、$i=1,\ldots,n$ は独立」と明記し、詳細解答でも尤度を積にできる根拠として再掲する必要がある。
2. `07_solutions.md` P3L-D01(5) — 改訂目的である逐行の詳細解答としては、識別の説明が結論だけで止まり、「分散を追加すれば分離できる」の代数がない。$m=E[X]=\alpha/\beta$、$v=\operatorname{Var}(X)=m+\alpha/\beta^2$、$q=v-m$ と置き、$\beta=m/q$、$\alpha=m^2/q$ と解く過程、およびモデル内では $m>0,q>0$ が必要なことまで示す必要がある。「全14問の方針・仮定・中間計算・結論を読者が補完せず追える」という今回の改訂基準をこの小問だけ満たしていない。

### minor

1. `07_solutions.md` の「独習用の逐行補足」P3L-B04 — (2)で必要な $\tau_1(0)$ の評価を補足層では計算せず、$\tau_1(3)$ だけを数値化している。$\tau_1(0)=1/(1+3e^{-9/2})\approx0.9677$ を示してから大小を結ぶと、比較の行間がなくなる。
2. `07_solutions.md` の「独習用の逐行補足」P3L-C01 — 問題(5)は $\bar X\xrightarrow{p}\alpha/\beta$ の証明を求めるが、補足層は $\widehat\alpha=\beta\bar X$ の確率収束を証明している。先行する短い解答には正しい上界があるため結論誤りではないが、補足層でも $P(|\bar X-\alpha/\beta|\geq\varepsilon)$ の上界を直接書くと設問対応が一対一になる。
3. `07_solutions.md` の「独習用の逐行補足」P3L-D01(4) — Gamma分布のLaplace変換を式として引用して代入しているが、初学者向けの逐行補足なら、事後密度に $e^{-\lambda}$ を掛けた積分をGamma積分で評価する1段を示すのが望ましい。
4. `07_solutions.md` P3L-C05(5) — 「成分数、分布形、ラベル交換、外れ値」などを列挙するだけで、何をどう確認するかの説明がない。少なくとも二つについて、成分数過大による不安定化、外れ値を小分散成分が拾う危険など、確認理由を一文ずつ補うと独習解説として完結する。

### 最新版30分ドリルの追加確認

- 追加確認日時: 2026-08-11T17:00:33+09:00以後
- 第1問のGamma積分では $t=(\beta+1)\lambda$ の置換とべきの係数を再計算し、周辺確率質量関数と一致することを確認した。
- 第2問の全期待値・全分散、第3問の推定量分散、第4問のChebyshev上界は、明記された独立性の下で正しい。
- 第5問は事後分布 $\operatorname{Gamma}(\alpha+x,\beta+1)$ の密度を用いた積分へ展開され、Gamma積分から掲載予測確率が得られる。
- 後半復帰用の与式は第2問の正しい結論であり、第1問から独立に第5問を開始できるとの案内も数理上妥当である。
- 最新版に対する追加指摘はない。章全体の判定件数は上記のままとする。
- 最新版での npm run validate: 成功（KaTeX strict 199 Markdown、本文検査222ファイル）

## 全面修正後の再査読

- 再査読日時: 2026-08-11T17:07:42+09:00
- 再査読範囲: 最新版の03・04・06・07・08、全14演習と全小問、C・D本番答案、30分ドリル
- npm run validate: 成功（KaTeX strict 199 Markdown、本文検査222ファイル）

### 旧指摘の解消確認

1. P3L-C04は $(X_i,Z_i)$ が互いに独立同分布であると問題文に明記され、積の尤度を使う仮定が確定した。
2. P3L-D01(5)は $m=\alpha/\beta$、$q=v-m=\alpha/\beta^2$ から $\beta=m/q$、$\alpha=m^2/q$ を導き、$m>0,q>0$ も明記した。
3. P3L-B04は $\tau_1(0)=1/(1+3e^{-9/2})=0.9677\ldots$ を示し、$\tau_1(3)$ との比較を数値で完結した。
4. P3L-C01は $\operatorname{Var}(\bar X)$ を求め、Chebyshev不等式を $\bar X$ へ直接適用した。
5. P3L-D01(4)は事後Gamma密度を用いた積分をGamma積分まで展開した。
6. P3L-C05(5)は成分数、分布形、共通分散、外れ値、ラベル対応について確認理由を説明した。

以上により、前回のmajor 2件・minor 4件そのものはすべて解消した。

### 定理・例題・ドリルの再計算

- 5定理の導出を再計算した。全期待値・全分散、Poisson--Gamma積分、正規混合の群間分散、密度版Bayes公式、完全・観測尤度はいずれも正しい。
- 例1の平均3・分散5、例2の周辺質量 $9(k+1)/4^{k+2}$・平均 $2/3$・分散 $8/9$・総和1、例3の責務 $0.599\ldots$、例4の二種類の尤度を再計算し、掲載結果と一致した。
- 30分ドリルは周辺化、モーメント、推定量分散、Chebyshev、事後予測積分を再計算し、全5問の掲載式が正しい。復帰用与式にも誤りはない。

### 新規major

1. 07_solutions.md の「問題別の詳細解答」を正本化した一方で、複数の明示的小問がその正本内では未完答である。P3L-C02(4)は $\pi=1/2$ の予測確率を
   $$
   \frac{8e+125e^{-5}}{8e^3+125}
   $$
   まで計算せず、(5)の更新理由も正本では述べていない。P3L-C03(1)は責務の定義式、(5)は $[0,1]$ への射影式を正本では書いていない。P3L-C04(2)は $n_1=0,n$ の境界解、(4)は現在値 $\pi=1/2$ の代入結果を正本では書いていない。P3L-D01(2)は $\operatorname{Var}(\bar X)=\alpha(\beta+1)/(n\beta^2)$、(3)はChebyshev上界を正本では示していない。一部は後段の圧縮された本番答案にあるが、詳細解答と本番答案を分離し、前者だけで全小問の方針・中間計算・結論を追えるという改訂目的を満たさない。各問題の詳細解答内で全小問を順番に完答する必要がある。

### 新規minor

1. 04_examples.md の例4だけ、例1--3にある「方針」と「検算」の明示がない。二つの尤度式は正しいが、独立性によって積を取れることと、所属既知・未知で因子が変わることを方針・検算として明記すると、今回の例題改訂方針が一貫する。

### 全面修正後判定

fatal: 0 / major: 1 / minor: 1

## 最終修正後の全文再査読

- 最終再査読日時: 2026-08-11T17:12:32+09:00
- 対象: 最新版03・04・06・07・08の全文、全14演習・全小問、C・D本番答案、30分ドリル
- 前回新規majorの解消: P3L-C02(4)は予測確率を $0.0791\ldots$ まで計算し、(5)は成分別尤度によって事前重みから事後責務へ更新される理由を明記した。P3L-C03(1)(5)、P3L-C04(2)(4)、P3L-D01(2)も詳細正本内で完答された。P3L-D01(3)は
  $$
  P(|\widehat\alpha-\alpha|\geq\varepsilon)
  \leq\frac{\alpha(\beta+1)}{n\varepsilon^2}\to0
  $$
  を示し、確率収束の結論まで記した。
- 前回新規minorの解消: 例4に方針と検算が追加され、独立性による積と $\pi=1$ の退化ケースを確認した。
- 全14問の最終式と本番答案の結論は一致する。新規の数理的・論理的指摘はない。
- npm run validate: 成功（KaTeX strict 199 Markdown、本文検査222ファイル）

### 最終判定

fatal: 0 / major: 0 / minor: 0

### 前回判定（履歴）

`fatal: 0 / major: 2 / minor: 4`
