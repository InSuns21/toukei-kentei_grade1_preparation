# E2-05 読者粒度レビュー

## 基準

- P3-02「主要な連続分布」と同じく、意味 → 具体例 → 一般式 → 導出の順を優先する。
- 初出語を既知扱いしない。
- 公式の丸暗記ではなく、どこから式が出るか追えること。
- 問題直後に詳細解答・本番答案・採点基準を置く。

## 確認結果

### 導入

炉内温度・センサーを例に、潜在状態と観測を分ける必要性から開始した。状態空間モデルの一般式をいきなり置く構成にはしていない。

### 初出語

状態、観測、状態方程式、観測方程式、システムノイズ、観測ノイズ、filtering、prediction、smoothing、イノベーション、Kalman gain を本文中で初出時に説明した。

### Kalman filter

`予測 → 観測予測 → イノベーション → gain → 更新` の順に構成し、Kalman gain を暗記公式として与えず、多変量正規分布の条件付き分布から導出した。

### スカラーから行列へ

先にスカラー形で重み付き平均として直感を与え、その後で行列形へ一般化した。行列形では次元表を追加し、積の意味を追えるようにした。

### 時系列との接続

AR(1)、AR(2) の状態空間表現を入れ、E2-03 から状態空間モデルへ自然に接続した。filtering / prediction / smoothing の違いも明示した。

### 発展範囲

イノベーション尤度、欠測時の更新スキップ、Gaussian 仮定を外した場合の解釈を補足した。公式範囲を逸脱した前提知識としては扱わず、本文内で導入した。

### 演習

Level A 4題 / B 4題 / C 4題 / D 1題 / 30分ドリルを配置。Advanced 30 状態空間モデル・1期先Kalman更新へ接続した。

## 結論

読者粒度は P3-02 基準を満たす。自動検証通過後に最終 PASS とする。
