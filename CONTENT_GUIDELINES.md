# 統計検定1級 教材共通規約

このファイルを、次の4教材に共通する**上位規約の正本**とする。

- `textbook/`：通常教材
- `statistical-mathematics/`：統計数理100大問
- `applied-rikou-80/`：統計応用（理工学）80大問
- `anki/`：Ankiカード

各教材のローカル規約は、その教材だけに必要な差分を定める。共通事項をローカル側で再定義しない。食い違いがある場合は、このファイルと下記の共通正本へ本文を合わせる。

## 1. 共通正本

| 対象 | 正本 |
|---|---|
| 用語・略語・日本語表記 | `references/terminology-guide.md` |
| 一般記号・数理記法 | `references/notation-guide.md` |
| 分布の台・母数・記号・密度/質量関数 | `references/distribution-notation-guide.md` |
| 演習の詳細解答粒度・定理適用・電卓基準 | `EXERCISE_GUIDELINES.md` |
| 通常教材の章構成 | `textbook/style-guide.md` |
| 通常教材固有の記法差分 | `textbook/notation.md` |
| Anki固有のカード記法差分 | `anki/notation.md` |

`EXERCISE_GUIDELINES.md` は統計数理100、理工80、通常教材の演習・詳細解答に適用する。Ankiは「1カード1論点」であり、20〜30分答案・採点基準・打ち切り判断は要求しない。ただし、途中式を暗記で飛ばさない、非自明な略語を本文で常用しない、分布の定義を曖昧にしないという原則は共有する。

## 2. 用語と略語

本文・問題文・詳細解答・本番答案・Ankiカードでは、統計検定1級の学習者が日本語だけで追える表記を優先する。

- `PDF` ではなく「確率密度関数」。
- `PMF` ではなく「確率質量関数」。
- `CDF` ではなく「累積分布関数」。
- `PGF` ではなく「確率母関数」。
- `MGF` ではなく「モーメント母関数」。
- `MLE` ではなく文脈に応じて「最尤推定量」または「最尤法」。
- `LRT` ではなく「尤度比検定」。
- `UMP`, `UMPU`, `UMVU` なども日本語正式名を本文の主表記とする。

英字略語は、出典名、ファイル名、安定ID、コード、数式記号、公式シラバス自体が英字を標準名としている場合を除き、本文の主表記にしない。英語名を示す教育的理由がある場合は初出で併記してよいが、その後の説明は日本語正式名を使う。

## 3. 記号と分布

同じ概念を教材ごとに別記号へしない。一般記号は `references/notation-guide.md`、分布固有の規約は `references/distribution-notation-guide.md` を共通正本とする。

- 正規分布の第2引数は分散とする。
- ガンマ分布は shape-rate を既定とし、密度を初出時に明記する。
- 幾何分布の台は既定では初成功までの試行回数 `1,2,...` とする。別規約が必要ならその場で明記する。
- 確率変数は大文字、観測値は小文字を基本とする。
- 指示関数は `\boldsymbol{1}_A` を基本とする。

### 3.1 定義・定理・命題での記号導入

定義・定理・命題・補題では、式に現れる記号を**初出時に文章で宣言してから使う**。学習者が前章や慣習から記号の意味を推測することを前提にしない。

特に、確率質量関数・確率密度関数・同時分布・母数・条件付け事象などは、式の直前で意味を明記する。

例：

- 離散型確率変数 $X$ の確率質量関数を $p_X(x)=P(X=x)$ とする。
- 連続型確率変数 $X$ の確率密度関数を $f_X(x)$ とする。
- 2変量連続分布では、$(X,Y)$ の同時確率密度関数を $f_{X,Y}(x,y)$ とする。
- 母数を使う場合は、$\theta\in\Theta$ のように母数空間も必要に応じて明記する。
- $H_1,\ldots,H_m$ を使う場合は「互いに排反で全事象を分割し、$P(H_i)>0$」など、定理で必要な条件を同時に書く。

例えば期待値の定義を

$$
E[X]=\sum_x x p_X(x)
$$

と書くなら、その直前に「離散型確率変数 $X$ の確率質量関数を $p_X(x)=P(X=x)$ とする」と置く。連続型なら「確率密度関数を $f_X(x)$ とする」と断ってから

$$
E[X]=\int x f_X(x)\,dx
$$

を使う。

同じ節の直前で明示済みの記号は繰り返し定義しなくてよいが、別の定義・定理へ移ったときに意味が曖昧になる記号は再度宣言する。`E`, `P`, `\operatorname{Var}` のような標準演算子そのものよりも、その演算子に入る確率変数、分布、関数、母数、集合の意味を優先して明示する。

この規約は `textbook/`、`statistical-mathematics/`、`applied-rikou-80/` に全面適用する。Ankiでは通常カードすべてに長い記号宣言を要求しないが、**定義・定理そのものを問うカード**では同じ規約に従う。

### 3.2 共通基礎定理を演習で前提にする場合

通常教材で体系的に導入した基礎定理を、統計数理100・理工80・通常教材の後続章で前提として使ってよい。ただし、詳細解答で完成式だけを無言で置かず、**使った定理・公式の名前と適用条件が追えるようにする**。

条件付きモーメントについては、通常教材 [P2-02 期待値・分散・共分散・母関数](textbook/volumes/01_probability/P2_02_期待値_分散_共分散_母関数/index.md) の次を共通の基礎説明とする。

- 条件付き期待値 $E[X\mid Z]$ とタワープロパティ
  $$
  E\{E[X\mid Z]\}=E[X].
  $$
- 条件付き分散と全分散公式
  $$
  \operatorname{Var}(X)
  =E\{\operatorname{Var}(X\mid Z)\}
  +\operatorname{Var}\{E[X\mid Z]\}.
  $$
- 条件付き共分散と全共分散公式
  $$
  \operatorname{Cov}(X,Y)
  =E\{\operatorname{Cov}(X,Y\mid Z)\}
  +\operatorname{Cov}\{E[X\mid Z],E[Y\mid Z]\}.
  $$

統計数理100・理工80でこれらを使うとき、問題自体が公式の証明を要求していなければ毎回の再証明は不要である。一方で、期待値なら可積分性、分散・共分散なら必要な二次モーメントの有限性など、適用条件を無視してよいという意味ではない。初学者が前提章へ戻れるよう、非自明な利用箇所では必要に応じて P2-02 へのリンクを置く。

有限分割 $H_1,\ldots,H_m$ に対する全期待値・全分散・全共分散は、群ラベル $Z$ を導入した上記一般形の特殊例として扱い、別の無関係な公式として重複定義しない。

## 4. Markdown・KaTeX

4教材ともGitHub Pagesで同じKaTeX系の記法を使う。

- インライン数式：`$...$`
- 別行数式：`$$...$$`
- 複数行：`$$\begin{aligned}...\end{aligned}$$`
- `\(...\)`, `\[...\]`, `align`, `equation`, 独自マクロ、外部LaTeXパッケージ依存は使わない。
- 行列、指示関数、添字、転置などはKaTeXで実際に表示できることをvalidationで確認する。

## 5. 解説粒度

統計数理100・理工80・通常教材の演習では、`EXERCISE_GUIDELINES.md` を共通基準とする。代表的な詳細解答の粒度は `statistical-mathematics/core/40_fisher_information_delta_mle_efficiency.md` を参照し、文字数ではなく**導出再現性**を合わせる。

主な判定基準は次の通り。

1. 何を出発式にしたか分かる。
2. 定理・公式の適用条件が分かる。
3. 採点対象の微分・積分・行列計算・平方完成・漸近展開を再現できる。
4. 問題で求める母関数・尤度・情報量などを既知公式として置かない。
5. 本番答案は圧縮しても、採点される導出の核を残す。

Ankiも「公式だけ提示して計算を省略する」カードを避け、短い具体例で本質的操作を最低1回実行する。ただし長大な論述答案へはしない。

### 5.1 教材本文に編集履歴を持ち込まない

学習者向けの本文・問題・詳細解答・本番答案には、**編集作業の事情や過去版との比較を書かない**。

禁止例：

- 「旧問題から差し替えた」
- 「他の問題との重複を避けるため変更した」
- 「以前はこの論点を扱っていた」
- 「今回の修正で追加した」
- 「レビュー指摘を受けて直した」

変更理由、重複整理、移行経緯、レビュー記録は、必要ならコミットメッセージ・Issue・監査用Markdownなどの**管理側の記録**に置く。教材本文は常に、現在の完成形だけを読んで自然に理解できる状態にする。

## 6. validation と audit の役割

- **validation**：壊れた数式、壊れたリンク、文字コード、必須メタデータなど、機械的に判定できる問題をブロックする。
- **audit**：解説粒度、用語統一、重複、学習順など、人間の判断を要する改善候補を非ブロッキングで抽出する。

Chatから教材本文を追記するとき、人手査読記録が未作成であることだけを理由に `npm run validate` を失敗させない。査読の有無と機械的妥当性を分離する。