# 解法の型

1. 経験CDFは指示変数の平均へ直す。
2. 逆関数法は $F^{-1}(u)=\inf\{x:F(x)\ge u\}$ を求め、$X=F^{-1}(U)$ とする。$F$ が連続で狭義増加の場合だけ、通常の方程式 $F(X)=U$ を解いてよい。
3. 棄却法は $M=\sup f/g$、受理率 $1/M$、受理後密度の順に示す。
4. Monte Carloは積分を期待値へ直し、不偏性、分散、標準誤差、必要標本数を計算する。
5. 乱数・近似値を答案へ丸投げせず、理論式と誤差評価を必ず残す。
