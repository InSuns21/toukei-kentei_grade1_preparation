# 例題

標本 $1,2,2,5$ の経験CDFは $x<1$ で0、$1\le x<2$ で$1/4$、$2\le x<5$ で$3/4$、$x\ge5$ で1。中央値は2。

指数乱数は $X=-\log U/\lambda$ としてもよい。一様性より $1-U$ と $U$ は同分布だからである。

$I=\int_0^1u^2du=1/3$ のMonte Carlo推定量は $N^{-1}\sum U_i^2$。分散は $(1/5-1/9)/N=4/(45N)$。
