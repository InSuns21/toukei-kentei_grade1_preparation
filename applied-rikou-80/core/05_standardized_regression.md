# Core 05 標準化重回帰・相関行列からの推定

- 安定ID: `RIKOU-CORE-05`
- 80大問 No.: 36
- 演習価値: S
- 難度: B
- 目安時間: 20〜25分
- 電卓: 四則演算・平方根までで完結

## 問題

$Y,X_1,X_2$ はすべて平均0、分散1に標準化されている。相関係数が

$$
r_{12}=0.5,\qquad r_{Y1}=0.7,\qquad r_{Y2}=0.6
$$

である。

1. 標準化偏回帰係数 $b_1,b_2$ を求めよ。
2. 重相関係数の二乗 $R^2$ を求めよ。
3. $X_1=1$, $X_2=-0.5$ の個体に対する標準化予測値を求めよ。
4. $X_2$ を固定したときの $Y$ と $X_1$ の偏相関係数を求めよ。
5. $X_1$ を $X_2$ で回帰したときの決定係数を用いて $X_1$ の VIF を求めよ。また、$r_{12}$ が1に近づくと偏回帰係数の推定が不安定になる理由を説明せよ。

## 詳細解答

### 1. 偏回帰係数

$$
R_{XX}=\begin{pmatrix}1&0.5\\0.5&1\end{pmatrix},\qquad
r_{XY}=\begin{pmatrix}0.7\\0.6\end{pmatrix}.
$$

標準化回帰係数は

$$
b=R_{XX}^{-1}r_{XY}.
$$

$$
R_{XX}^{-1}=\frac1{0.75}\begin{pmatrix}1&-0.5\\-0.5&1\end{pmatrix}.
$$

したがって

$$
b_1=\frac{0.7-0.3}{0.75}=\frac{8}{15},
\qquad
b_2=\frac{0.6-0.35}{0.75}=\frac13.
$$

### 2. 決定係数

$$
R^2=r_{XY}^\top b
=0.7\frac{8}{15}+0.6\frac13
=\frac{43}{75}.
$$

### 3. 予測値

$$
\hat Y=\frac{8}{15}-\frac12\frac13
=\frac{11}{30}.
$$

### 4. 偏相関

$$
r_{Y1\cdot2}
=\frac{0.7-0.6\cdot0.5}{\sqrt{(1-0.6^2)(1-0.5^2)}}
=\frac{0.4}{\sqrt{0.48}}.
$$

一般電卓なら平方根まで評価できるが、この形までで統計的立式は完成している。

### 5. VIF と多重共線性

$X_1$ を $X_2$ だけで回帰したとき

$$
R_1^2=r_{12}^2=0.25.
$$

したがって

$$
\boxed{\operatorname{VIF}_1=\frac1{1-R_1^2}=\frac4{3}}.
$$

$r_{12}\to1$ では $R_{XX}$ の行列式 $1-r_{12}^2\to0$ となり、$R_{XX}^{-1}$ の成分が大きくなる。そのため標本相関のわずかな変動が偏回帰係数を大きく動かし、標準誤差も増大する。

## 本番答案

$$
b=R_{XX}^{-1}r_{XY}
=\begin{pmatrix}8/15\\1/3\end{pmatrix},
\quad
R^2=43/75,
\quad
\hat Y=11/30.
$$

$$
r_{Y1\cdot2}=\frac{0.4}{\sqrt{0.48}}.
$$

また

$$
\operatorname{VIF}_1=\frac1{1-0.5^2}=\frac43.
$$

$r_{12}\to1$ では $\det R_{XX}\to0$ となり、逆行列が不安定になるため多重共線性が生じる。

## 採点基準

- (1) 回帰係数: 6点
- (2) $R^2$: 3点
- (3) 予測値: 2点
- (4) 偏相関: 4点
- (5) VIF と不安定性: 5点

20分経過時は小数化を止め、行列式 $1-r_{12}^2$ と VIF の意味まで答案に残す。
