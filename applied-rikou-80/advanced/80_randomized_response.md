# Advanced 80 確率化回答法・最尤推定量・不偏補正

- 安定ID: `RIKOU-ADVANCED-80`
- 80大問 No.: 80
- 演習価値: S
- 難度: A
- 目安時間: 25〜30分

## 問題

母集団で敏感な属性を持つ割合を $\pi$ とする。回答者は確率 $p>1/2$ で「私は属性を持つ」という文、確率 $1-p$ でその否定文に答え、真ならYesと回答する。Yes確率を $\theta$ とする。

1. $\theta$ を $\pi,p$ で表せ。
2. Yes数 $Y\sim\operatorname{Binomial}(n,\theta)$ として、制約を無視した内部解 $\tilde\pi$ を求めよ。
3. 母数制約 $0\le\pi\le1$ を考えた最尤推定量を述べよ。
4. $\tilde\pi$ が不偏であることと、その分散を示せ。
5. $p\to1/2$ で推定精度が悪化する理由と、プライバシーとのトレードオフを説明せよ。

## 詳細解答

$$
\theta=p\pi+(1-p)(1-\pi)=(1-p)+(2p-1)\pi.
$$

二項尤度の内部最尤推定量は $\hat\theta=Y/n$ なので

$$
\tilde\pi=\frac{Y/n-(1-p)}{2p-1}.
$$

ただし $\pi\in[0,1]$ なので厳密な制約付き最尤推定量は

$$
\hat\pi=\min\{1,\max(0,\tilde\pi)\}.
$$

線形推定量 $\tilde\pi$ は $E(Y/n)=\theta$ から不偏で

$$
\operatorname{Var}(\tilde\pi)=\frac{\theta(1-\theta)}{n(2p-1)^2}.
$$

一方、境界へ切り詰めた最尤推定量は一般には厳密不偏ではない。$p\to1/2$ では分母が0へ近づき情報が失われるが、回答と真の属性の対応が曖昧になりプライバシーは強くなる。

## 本番答案

$\theta=(1-p)+(2p-1)\pi$。内部解は $\tilde\pi=[Y/n-(1-p)]/(2p-1)$、制約付き最尤推定量はこれを$[0,1]$へ切り詰める。$\tilde\pi$ は不偏で分散 $\theta(1-\theta)/[n(2p-1)^2]$。

## 採点基準

- Yes確率: 4点
- 内部解: 4点
- 制約付き最尤推定量: 4点
- 不偏性・分散: 5点
- トレードオフ: 3点

25分経過時は「不偏なのは切り詰め前の線形推定量」と明記する。
